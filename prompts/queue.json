{
  "project": "modrule-sync",
  "inFlight": null,
  "items": [],
  "roadmap": [],
  "shipped": [
    {
      "id": "gemini-claude-md-audit-modrule-sync",
      "tag": "chore",
      "title": "Gemini consistency audit -- modrule-sync CLAUDE.md",
      "doc": "CLAUDE.md",
      "why": "Both findings real, applied directly: (1) 'sibling vortex-collection-tools repo' was technically imprecise -- the two projects sit under different parent folders (skyrim-modding/ vs vortex-tools/), so 'sibling' could misread as same-parent-directory when the real paths given are already correct workspace-relative paths; clarified the wording without changing any path. (2) 'gitignore it once real history exists' gave no concrete trigger -- replaced with a real one: add it to .gitignore the first time a real handoff actually gets written here, in that same commit."
    },
    {
      "id": "build-modrulesync-v1",
      "tag": "feature",
      "title": "Build ModruleSync v1 -- standalone static tool (separate repo, not this app)",
      "doc": "match-engine.js, app.js, index.html, styles.css",
      "why": "Independently verified: match-engine.js implements every spec tier + the canonical-name rule correctly (read in full), including a real correctness fix beyond spec (removeCandidateEverywhere, preventing two rows from double-claiming the same author entry). Mockup class names confirmed carried into index.html/styles.css. Handoff documents genuine live browser verification (headless Node harness for the pure matching logic, a real Chrome pass for the full interactive UI, one real bug caught and fixed live). Commit 61c758e, pushed. GitHub Pages deliberately not enabled yet -- next step."
    }
  ]
}
